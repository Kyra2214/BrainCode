# PolicyDecision

Contract produced by policy evaluation.

Required: decision id, run/task references, actor, capability, risk class, decision, approval/sandbox flags, network/filesystem constraints, budget, expiry and reason.
