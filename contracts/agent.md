# Agent

Contract describing an agent capability profile.

Required: id, name, capabilities, specialties, limitations, risk class, required permissions, preferred providers, fallback agents and sandbox requirement.
